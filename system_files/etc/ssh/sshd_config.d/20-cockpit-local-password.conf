# Permit Cockpit's local web-service container to authenticate against the host
# without enabling password authentication from the LAN/WAN.
Match Address 127.0.0.1,::1
    PasswordAuthentication yes
